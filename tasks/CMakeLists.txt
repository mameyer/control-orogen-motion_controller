# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(motion_controllerTaskLib)
ADD_LIBRARY(${MOTION_CONTROLLER_TASKLIB_NAME} SHARED 
    ${MOTION_CONTROLLER_TASKLIB_SOURCES})
add_dependencies(${MOTION_CONTROLLER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${MOTION_CONTROLLER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${MOTION_CONTROLLER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${MOTION_CONTROLLER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MOTION_CONTROLLER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${MOTION_CONTROLLER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${MOTION_CONTROLLER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MOTION_CONTROLLER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MOTION_CONTROLLER_TASKLIB_HEADERS}
    DESTINATION include/orocos/motion_controller)

